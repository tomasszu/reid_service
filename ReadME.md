> This is the MQTT Implementation branch!!

# For a proper README inspect the main_kafka branch.

That branch also contains the Kafka implementation. Do NOT merge these branches, they are different implementations. The proper readme just awkwardly stayed on the other one.